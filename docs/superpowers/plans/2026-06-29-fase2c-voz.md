# Fase 2C — Voz (dictado + lectura) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the assistant hands-free — dictate a question (STT, auto-send) and have replies spoken aloud (TTS) with a mute toggle and a per-message play button.

**Architecture:** Three thin, fakeable units (`SpeechToTextService`, `TtsService`, `VoicePrefs`) composed by a `VoiceController` (`ChangeNotifier`, 6th provider in `main`). `ChatPage` is the only consumer. Gemma/stub inference and persistent conversations are untouched — voice wraps around them.

**Tech Stack:** Flutter, Provider, `speech_to_text`, `flutter_tts`, `shared_preferences`.

## Global Constraints
- Package `aplication_tesis`. Spanish-first; **all styling from the Theme** (no hardcoded colors); reuse existing design-system widgets.
- Disease-class contract `'healthy'/'mancha_negra'/'rona'` intact. l10n ES/EN via ARB + `flutter gen-l10n`.
- **No `Co-Authored-By: Claude`** trailer in commits. Work on `main`.
- Keep all existing tests green (currently 99). `flutter analyze` must stay clean.
- TTS default language es-ES; follow app locale (es-ES / en-US). Mic permission requested on first use; deny → text chat still works.
- Verify the APK still builds and 16 KB Play compliance holds after adding native plugins.

---

### Task 1: Dependencies + permissions + VoicePrefs

**Files:**
- Modify: `pubspec.yaml` (add `speech_to_text`, `flutter_tts`)
- Modify: `android/app/src/main/AndroidManifest.xml` (RECORD_AUDIO + queries)
- Modify: `ios/Runner/Info.plist` (usage strings)
- Create: `lib/features/assistant/data/voice_prefs.dart`
- Test: `test/features/assistant/voice_prefs_test.dart`

**Interfaces:**
- Produces: `class VoicePrefs { Future<bool> getAutoSpeak(); Future<void> setAutoSpeak(bool value); }` — SharedPreferences key `'assistant_auto_speak'`, default `true`.

- [ ] **Step 1:** Add to `pubspec.yaml` dependencies: `speech_to_text: ^7.0.0` and `flutter_tts: ^4.2.0` (if a version is unresolvable, run `flutter pub get` and accept pub's suggested compatible version; record it). Run `flutter pub get`.
- [ ] **Step 2:** `AndroidManifest.xml` — inside `<manifest>` add `<uses-permission android:name="android.permission.RECORD_AUDIO"/>`; add (merge if a `<queries>` already exists) a `<queries>` block with two `<intent>`s: action `android.speech.RecognitionService` and action `android.intent.action.TTS_SERVICE`. Do NOT remove the existing OpenCL `<uses-native-library>` entries.
- [ ] **Step 3:** `ios/Runner/Info.plist` — add `NSMicrophoneUsageDescription` = "avocadoIA usa el micrófono para dictar tus preguntas al asistente." and `NSSpeechRecognitionUsageDescription` = "avocadoIA convierte tu voz en texto para el asistente."
- [ ] **Step 4 (TDD):** Write `voice_prefs_test.dart` using `SharedPreferences.setMockInitialValues({})`: default `getAutoSpeak()` → `true`; after `setAutoSpeak(false)`, `getAutoSpeak()` → `false`. Run → fails.
- [ ] **Step 5:** Implement `VoicePrefs` (mirror `assistant_prefs.dart` style). Run tests → pass.
- [ ] **Step 6:** `flutter analyze` clean; `flutter build apk --debug` succeeds (confirms native plugins link).
- [ ] **Step 7:** Commit `feat(voice): deps + mic/tts permissions + VoicePrefs`.

---

### Task 2: Voice services + VoiceController

**Files:**
- Create: `lib/features/assistant/domain/voice_services.dart` (abstract `SpeechToTextService`, `TtsService`)
- Create: `lib/features/assistant/data/real_speech_to_text_service.dart`
- Create: `lib/features/assistant/data/real_tts_service.dart`
- Create: `lib/features/assistant/presentation/providers/voice_controller.dart`
- Modify: `lib/main.dart` (register `VoiceController` as 6th provider; init)
- Test: `test/features/assistant/voice_controller_test.dart`

**Interfaces:**
- Produces:
  - `abstract class SpeechToTextService { Future<bool> init(); bool get isAvailable; Future<void> startListening({required void Function(String partial) onPartial, required void Function(String finalText) onFinal, String localeId = 'es_ES'}); Future<void> stop(); }`
  - `abstract class TtsService { Future<void> init(); Future<void> speak(String text, {String languageTag = 'es-ES'}); Future<void> stop(); set onSpeakingChanged(void Function(bool) cb); }`
  - `class VoiceController extends ChangeNotifier { VoiceController(this._stt, this._tts, this._prefs); bool get isAvailable; bool get isListening; bool get isSpeaking; bool get autoSpeak; String get partialText; Future<void> init(); Future<void> toggleAutoSpeak(); Future<void> startDictation({required void Function(String finalText) onFinal, String localeId}); Future<void> stopDictation(); Future<void> speak(String text, {String languageTag}); Future<void> stopSpeaking(); }`
- Consumes: `VoicePrefs` (Task 1).

- [ ] **Step 1:** Create `voice_services.dart` with the two abstract classes above.
- [ ] **Step 2 (TDD):** `voice_controller_test.dart` with FAKE `SpeechToTextService` + FAKE `TtsService` + in-memory `VoicePrefs` (or a fake). Tests:
  - `init()` loads `autoSpeak` from prefs (true by default) and calls `stt.init()`/`tts.init()`.
  - `toggleAutoSpeak()` flips `autoSpeak` and persists it.
  - `startDictation(onFinal: cb)`: when the fake STT emits a final result, `cb` is called with that text and `isListening` returns to false.
  - `speak()` sets `isSpeaking` true then false via the fake's `onSpeakingChanged`.
  - `startDictation()` first calls `tts.stop()` (dictation cancels any speech).
  Run → fails.
- [ ] **Step 3:** Implement `VoiceController` (wires the services, owns `autoSpeak`/`isListening`/`isSpeaking`/`partialText`, notifies). Run tests → pass.
- [ ] **Step 4:** Implement `RealSpeechToTextService` using `package:speech_to_text`: `init()` → `_speech.initialize(onStatus:, onError:)` (returns availability + triggers mic permission); `startListening` → `_speech.listen(onResult: (r) { onPartial(r.recognizedWords); if (r.finalResult) onFinal(r.recognizedWords); }, localeId: localeId, listenOptions: ...)`; `stop()` → `_speech.stop()`; `isAvailable` from initialize result.
- [ ] **Step 5:** Implement `RealTtsService` using `package:flutter_tts`: `init()` → set default language + `setSpeechRate(0.5)`; register `setStartHandler`/`setCompletionHandler`/`setCancelHandler`/`setErrorHandler` to drive `onSpeakingChanged`; `speak(text, languageTag)` → `setLanguage(languageTag)` then `speak`; `stop()` → `stop`.
- [ ] **Step 6:** `main.dart` — register `ChangeNotifierProvider(create: (_) => VoiceController(RealSpeechToTextService(), RealTtsService(), VoicePrefs())..init())` as the 6th provider (keep the existing 5). Import the files.
- [ ] **Step 7:** `flutter analyze` clean; `flutter test` all pass. Commit `feat(voice): STT/TTS services + VoiceController`.

---

### Task 3: ChatPage integration (dictation, mute toggle, per-message play, auto-read) + l10n

**Files:**
- Modify: `lib/features/assistant/presentation/pages/chat_page.dart`
- Modify: `lib/l10n/app_en.arb`, `lib/l10n/app_es.arb` (+ regen)
- Test: `test/features/assistant/chat_page_voice_test.dart`

**Interfaces:**
- Consumes: `VoiceController` (Task 2) via `context.read/watch`; `AssistantProvider.send` (existing).

- [ ] **Step 1:** Replace the `// Fase 2C: micrófono` placeholder with a **mic IconButton** in the input row. Tap when not listening → `voice.startDictation(onFinal: (t) { if (t.trim().isNotEmpty) context.read<AssistantProvider>().send(t); }, localeId: <es_ES/en_US from app locale>)`. While `voice.isListening`: show a "stop" mic (filled/animated, theme color) + show `voice.partialText` as a hint above/in the input. Tap while listening → `voice.stopDictation()`.
- [ ] **Step 2:** Add a **🔊/🔇 IconButton** to the ChatPage AppBar bound to `voice.autoSpeak` → `voice.toggleAutoSpeak()`. Tooltip localized.
- [ ] **Step 3:** On each **assistant** message bubble add a small **play/stop IconButton**: if `voice.isSpeaking` (and this is the speaking message) → stop icon → `voice.stopSpeaking()`; else play icon → `voice.speak(message.text, languageTag: <locale>)`.
- [ ] **Step 4:** **Auto-read once per reply:** in a `Consumer`/listener, when `assistantProvider.isThinking` is false and the newest assistant message's `timestamp` differs from the last one ChatPage spoke, and `voice.autoSpeak` is true → `voice.speak(text)` and record that timestamp in ChatPage state. Cancel/stop speech in `dispose()` and when dictation starts.
- [ ] **Step 5:** l10n — add to BOTH arbs (template `app_en.arb` with `@` desc) + `flutter gen-l10n`: `voiceDictate` ("Dictate"/"Dictar"), `voiceListening` ("Listening…"/"Escuchando…"), `voiceStop` ("Stop"/"Detener"), `voicePlay` ("Play"/"Reproducir"), `voiceAutoReadOn` ("Auto-read on"/"Lectura automática activada"), `voiceAutoReadOff` ("Auto-read off"/"Lectura automática desactivada"), `micDenied` ("Microphone permission denied"/"Permiso de micrófono denegado"), `voiceUnavailable` ("Voice not available on this device"/"Voz no disponible en este dispositivo"). Use `AppLocalizations.of(context)` with hardcoded fallbacks.
- [ ] **Step 6 (TDD):** `chat_page_voice_test.dart` with a FAKE `VoiceController` (override the controller with fakes; provide via `MultiProvider` in the test): mic button visible; tapping the AppBar toggle calls `toggleAutoSpeak`; an assistant bubble shows a play control; tapping it calls `voice.speak`. Avoid real plugins. Keep existing chat_page tests green.
- [ ] **Step 7:** `flutter analyze` clean; `flutter test` all pass; `flutter build apk --debug` succeeds. Commit `feat(voice): ChatPage dictation + speak + auto-read`.

---

## Notes for the implementer
- Real STT/TTS only work on a device — tests use fakes. After Task 3, the user validates on the Pixel (dictation auto-sends; replies are spoken; toggle mutes; play works; mic-deny path is graceful).
- Derive the locale tag from the app's `LocaleProvider`/`Localizations.localeOf(context)`: Spanish → STT `es_ES`, TTS `es-ES`; English → `en_US` / `en-US`.
- Stop TTS when leaving ChatPage (`dispose`) and before starting dictation.
