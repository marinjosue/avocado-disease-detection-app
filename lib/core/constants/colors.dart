import 'package:flutter/material.dart';

class AppColors {
  AppColors._();
  
  // Colores primarios
  static const Color primary = Color(0xFF2E7D32); // Verde aguacate
  static const Color primaryLight = Color(0xFF66BB6A);
  static const Color primaryDark = Color(0xFF1B5E20);
  
  // Colores de acento
  static const Color accent = Color(0xFF8BC34A);
  static const Color accentLight = Color(0xFFDCEDC8);
  
  // Colores de fondo
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Color(0xFFFFFFFF);
  
  // Colores de texto
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);
  
  // Colores para estados de enfermedad
  static const Color healthy = Color(0xFF4CAF50);
  static const Color manchaNegra = Color(0xFF424242); // Negro/gris oscuro
  static const Color rona = Color(0xFFFF6F00); // Naranja oscuro
  
  // Colores de estado
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFA726);
  static const Color error = Color(0xFFE53935);
  static const Color info = Color(0xFF42A5F5);
  
  // Colores adicionales
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color grey = Color(0xFF9E9E9E);
  static const Color greyLight = Color(0xFFE0E0E0);
}
